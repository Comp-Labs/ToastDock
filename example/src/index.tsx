import React from 'react'
import ReactDOM from 'react-dom/client'
import { ToastDock } from '@techfiddle/toastdock'

const root = ReactDOM.createRoot(document.getElementById('root') as HTMLElement)
root.render(
  <div>
    <ToastDock />
  </div>>,
)
